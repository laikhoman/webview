apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            storeFile file('C:\\Users\\HANDYLAI\\Downloads\\IndoSlot.jks')
            storePassword 'IndoSlotGacor'
            keyAlias 'INDOSLOTGACOR'
            keyPassword 'IndoSlotGacor'
        }
    }
    compileSdkVersion 32

    defaultConfig {
        applicationId "com.journaldev.webview"
        minSdkVersion 21
        targetSdkVersion 32
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    //noinspection GradleCompatible
    implementation 'com.android.support:appcompat-v7:23.0.1'
}
